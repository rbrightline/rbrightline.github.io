# json

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build json` to build the library.

## Running unit tests

Run `nx test json` to execute the unit tests via [Vitest](https://vitest.dev/).
